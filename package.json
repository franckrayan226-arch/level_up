{
  "name": "monolith",
  "version": "1.0.0",
  "description": "MONOLITH Editorial — Backend API + Admin Dashboard + React Frontend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "cd FRONTEND && npm install && ./node_modules/.bin/vite build"
  },
  "dependencies": {
    "cloudinary": "^1.41.0",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "mongodb": "^6.3.0",
    "multer": "^1.4.5-lts.1",
    "uuid": "^9.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}